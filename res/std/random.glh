vec2 seed = vec2(302405.0, 20335.0);

float rand()
{
    return (fract(sin(dot(seed.xy ,vec2(12.9898,78.233))) * 43758.5453) - 0.5) * 2.0;
}

vec2 randVec2()
{
    vec2 v;
	v.x = rand();
	seed = vec2(v.x, seed.x);
	v.y = rand();
	seed = vec2(v.y, v.x);
	return v;
}